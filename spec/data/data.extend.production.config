{
    env: 'production',
    server: {
        id: server,
        protocol: 'https',
        domain: 'example.com',
        port: 80,
        url: `${protocol}://${domain}${port === 80 ? '' : ':' + port}`
    }
}
