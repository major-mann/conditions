{
    query: {
        url: `${db.mongo.url}/myDatabase`
    },
    session: {
        url: `${db.redis.url}/1`,
        options: db.redis.options
    },
    cache: {
        url: `${db.redis.url}/2`,
        options: db.redis.options
    }
}
